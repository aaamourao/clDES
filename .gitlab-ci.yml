image: fedora:rawhide

pages:
  script:
  - dnf update
  - dnf install -y cmake
  - dnf install -y doxygen
  - dnf install -y ttf-freefont graphviz mscgen
  - mkdir -p build && cd build
  - cmake ..
  - make doc
  - mv doc/html/ public/
  artifacts:
    paths:
    - public
  only:
  - master
